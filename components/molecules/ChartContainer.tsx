import { JSXElementConstructor, ReactElement, ReactNode } from "react"

const ChartContainer = (children: any) => {
  return <div className="h-40 bg-white rounded-xl">{children}</div>
}

export default ChartContainer
